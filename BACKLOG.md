# Portfolio Backlog

Tracks deliberate deferrals on davedomingo.io — things the v2 research architecture
calls for that were consciously postponed, not missed. Distinct from decisions we
made to permanently deviate from the research spec (those get a note in
`docs/portfolio_plan.md` / the `project-portfolio` memory instead, since they're not
"still to do").

Check this file during any future v2-alignment pass so a deferred item doesn't get
re-flagged as a fresh gap.

---

## Open

- **Resume link/route (hero secondary CTA + footer deeper-channels link).**
  Spec: Compendium v2 §6.4 Layer 1 (`[View Selected Work] [Resume] [Contact]`) and
  Layer 6 (Resume among deeper channels). Deferred 2026-08-25 — no generic resume
  built yet; the resume(s) that exist are role/JD-tailored output from the pipeline,
  not a standalone portfolio-facing document. Add the link once a generic version
  exists.
